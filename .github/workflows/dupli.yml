name: Code Duplication Check

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  check-duplicates:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Install jscpd
        run: npm install -g jscpd
        
      - name: List files
        run: |
          echo "Project contents:"
          ls -la
          
      - name: Run duplication check
        run: |
          jscpd . \
            --ignore "**/*.test.js,**/*.spec.js,node_modules/**,dist/**,build/**,.git/**" \
            --reporters html,json,console \
            --format "javascript,typescript,jsx,tsx,css,scss,html,md" \
            --output ./jscpd-report \
            --threshold 1 \
            -d
        continue-on-error: true
            
      - name: Upload report as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jscpd-report
          path: ./jscpd-report
          if-no-files-found: warn
